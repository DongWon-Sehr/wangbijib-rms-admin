# 왕비집 예약관리 시스템 v2.1.2 기능명세서

## 1. 개요 (Overview)

* **프로젝트명**: 왕비집 예약관리 시스템 고도화
* **목적**: 왕비집 전 지점의 예약 관리, 슬롯 최적화, 예약금 관리 및 고객 응대 자동화 효율성 증대.
* **플랫폼**: Google Workspace 기반 웹 애플리케이션 (Google Sheets + Google Apps Script).
* **배포 설정**:
  * 실행 권한: `Me` (소유자 권한으로 스크립트 실행)
  * 접근 권한: `Every Google Account` (모든 구글 계정 사용자 접근 가능 - 내부 인증 로직으로 통제)
* **핵심 가치**: "예약 슬롯의 유기적 확보" (예약 확정 시 위젯 캘린더 슬롯을 자동으로 확보하여 매출 증대 기여).

## 2. 시스템 아키텍처 & 기술 스택

### 2.1. Frontend (SPA)
* **Framework**: `Vue.js 3` (Composition API, CDN 방식).
* **CSS Framework**: `Tailwind CSS` (CDN 방식).
* **Icons**: `Phosphor Icons`.
* **Rich Text Editor**: `Quill.js` (메일 템플릿 편집용).
* **Notification**: KaKao JS SDK (알림톡 전송).
* **타임존 고정 (v2.0.81)**: 브라우저 타임존과 무관하게 앱의 모든 날짜/시간을 앱 고정 타임존(KST, `javascript.html`의 `APP_TZ_OFFSET_HOURS`/`APP_TZ_SUFFIX` 상수)으로 해석·표시. 해외 접속 시 예약시간 드롭다운·예약 표시 시간·슬롯 마감 날짜가 현지 시간으로 밀리던 버그 수정.
  * 표시: 클라이언트 `Util.formatDate`가 로컬 `getHours()` 대신 epoch에 고정 오프셋(+9h)을 더한 뒤 `getUTC*()`로 포맷.
  * 저장: 예약 저장 시 `Util.parseInAppTz(date, time)`로 `+09:00` 오프셋을 명시하여 ISO 변환 (기존엔 브라우저 로컬 타임존으로 해석).
  * 필터/날짜 연산: 날짜 범위 필터는 KST 자정 epoch 기준, `addDays`/`changeSlotDate`는 epoch(±86400000ms) 연산으로 변경.
  * **시간 전용 셀 직렬화 (v2.0.81, Backend)**: 시트의 시간 전용 값(`slot_master.time`)은 1899-12-30 기준 Date로 반환되는데, 1899년 서울 타임존이 LMT +08:27:52라 `toISOString()` 시 32분(환경에 따라 2분) 드리프트 발생. `Util.getSheetDataAsObjects`가 1970년 이전 Date를 `Utilities.formatDate`로 벽시계 `HH:mm` 문자열로 직렬화하도록 수정 (서버/클라이언트 `formatDate` 모두 `HH:mm` 문자열 패스스루 지원).
* **앱 버전 표기 (v2.0.75)**: `Config.APP_VERSION`을 단일 소스로 사용. 배포 시 이 값만 직접 올리면 `doGet`이 index 템플릿(`data-app-version`)과 브라우저 탭 타이틀에 반영하여 GNB 타이틀 옆에 `v{{ appVersion }}`으로 표시함. (별도 빌드/버전 동기화 스크립트 없음, 배포는 `clasp push` 단독)
* **구조 전략**:
  * **Template 분리**: `index.html` 유지보수를 위해 `LayoutHeader.html`, `ViewList.html`, `ModalReservation.html` 등 컴포넌트 단위로 파일 분리 후 `include()` 처리.
  * **데이터 캐싱 & 동기화**:
    * **Initial Load**: 최초 진입 시 스피너를 띄우고 전체 예약 데이터 및 설정(Branch Map, Slot Map, Mail Templates)을 메모리에 로드.
    * **Reactive Update**: 데이터 수정 시 로컬 상태(State)를 즉시 업데이트하여 UI 반응성 확보 후 백그라운드 서버 동기화.
    * **Background Sync**: 1분 주기로 서버 데이터를 조용히 페치(Fetch)하여 변경 사항을 로컬 상태에 병합. 
      * 예약 상세 모달이 열려 있는 경우에도 작동하여, 누락되었던 `email_thread_id`가 보정되면 메일 발송 버튼을 자동 활성화하며, 헤더 우측에 1초 단위의 동기화 카운트다운 UI를 제공하여 가시성을 높임. 
      * 데이터 수정 뮤테이션(`isMutating = true`) 중에는 경합 조건 방지를 위해 동기화가 일시 정지됨.
      * **구버전 응답 폐기 (v2.0.75)**: 동기화 요청이 이미 날아간 상태에서 취소/저장 등 뮤테이션이 완료되면, 늦게 도착한 동기화 응답(뮤테이션 이전의 구버전 데이터)이 optimistic 반영분을 덮어쓰지 않도록 `lastMutationCompletedAt` 타임스탬프 비교로 해당 응답을 폐기함. (취소 처리 직후 화면이 이전 상태로 되돌아 보이던 경합 버그 수정)
      * **상태 보존(Race Condition 방지)**: 사용자가 예약을 클릭하여 로컬에서 읽음(`is_read = true`) 처리가 선행된 경우, 백그라운드 동기화 시 서버가 과거 상태(`false`)를 반환하더라도 덮어쓰지 않고 로컬의 읽음 상태를 보존함.
  * **모바일 최적화**: 터치 기반의 **당겨서 새로고침(Pull-to-Refresh)** 기능 구현.
  * **모바일 레이아웃 정비 (v2.0.78)**: 최신 기기 규격(Galaxy S25/S25 Ultra, iPhone 16/16 Pro Max) 기준으로 전 화면·모달·컨펌창의 가로 오버플로 및 어색한 줄바꿈 제거.
    * GNB: 모바일 타이틀 `whitespace-nowrap` + 로고 축소(h-10) + 아이콘 간격 축소로 "왕비집 예약" 한 줄 고정.
    * 예약 모달: '신규 스레드 생성' 버튼 줄바꿈 방지(스레드 ID 텍스트가 대신 truncate).
    * 설정 탭바: 탭 라벨이 한 글자씩 세로로 깨지던 문제 수정 — 줄바꿈 금지 + 가로 스크롤 탭바로 변경.
    * 슬롯 관리 컨트롤(모바일): 날짜 네비를 ◀ [날짜 입력] ▶ 한 줄로 통합(중복 볼드 날짜 텍스트는 데스크톱 전용), 지점 선택 풀폭, 커스텀/기본 슬롯 설정 버튼 2열 배치.
    * GNB 디테일 (v2.0.79): 리프레시 카운트 숫자와 초 단위(s) 사이 자간 확보, 벨/햄버거 버튼 패딩·간격 축소, 모바일에서도 앱 버전을 타이틀 위에 소형 표기(데스크톱은 기존 위치 유지).
    * 모달 스크롤 영역 터치 고정 (v2.0.79): `touch-action: pan-y` + `overflow-x: hidden`으로 모달 본문/리스트/알림 목록의 좌우 스와이프 시 콘텐츠 움찔거림 제거 (세로 스크롤만 허용).
    * GNB 풀폭 정렬 (v2.0.80): 헤더의 `container`(넓은 화면에서 1280~1536px 폭 제한 + 중앙 정렬) 클래스를 `w-full`로 교체하여, 와이드 모니터에서 GNB 콘텐츠가 중앙으로 쏠리던 문제 수정 — 본문 영역과 동일하게 화면 좌우 끝 기준 정렬.

### 2.2. Backend (Google Apps Script)
* **패턴**: `Singleton Pattern` 적용. `Config.gs`, `Util.gs`를 포함한 모든 서비스 파일은 `const` Service = {} 객체 리터럴 구조로 변경하여 인스턴스 관리 비용 제거. (단, `ReservationBatch.gs`, `WebApi.gs`는 함수형 유지)
* **API Wrapper 패턴**: `WebApi.gs` 내 `_executeApi(apiName, action, params)` 래퍼 함수 도입.
  * 모든 API 요청의 진입/성공/실패/에러를 통일된 포맷으로 로깅.
  * **Error Handling**: Service 계층에서 에러 발생 시 래퍼가 포착하여 표준 실패 응답 반환.
* **데이터 액세스**: **Header Name 기반 Object Mapping** (ORM 유사 방식) 적용. `Util.getSheetDataAsObjects(sheetName)`.
* **인증(Auth)**: **Google Session 인증** (`Session.getActiveUser().getEmail()` 사용). 기존 비밀번호 방식 폐기.
* **템플릿 엔진**: **구글 시트(`mail_template`)**를 DB로 사용하여 HTML 템플릿을 관리. WebApi를 통해 조회 및 업데이트.

### 2.3. 디자인 시스템 (Color Palette)
* **Main Color (BI Copper `#C16E36`, v2.0.75)**: Tailwind CDN `tailwind.config`의 `primary` 팔레트(#6a3a22~#fdf6f0, BI 컬러 기반 코퍼 스케일)로 정의. GNB·버튼·타이틀·포커스링 등 브랜드 요소는 모두 `primary-*` 클래스를 사용하므로 **index.html의 팔레트 값만 교체하면 전체 테마가 일괄 변경**됨. (기존 Royal Red `#8B0000`에서 교체)
* **노란계열 분리 원칙 (v2.0.75)**: 코퍼 프라이머리와 웜톤 겹침을 피하기 위해 예약 모달의 예약금 카드를 앰버에서 뉴트럴(스톤)로 변경, **옐로 계열은 카카오톡 영역 전용 시그니처**로 유지.
* **BI 로고 (v2.0.75)**: 스크립트 소유 계정 Drive의 로고 파일(`Config.LOGO_FILE_ID`)을 `doGet`에서 base64 data URI로 읽어(CacheService 6시간 캐시) GNB 로고로 표시. 로드 실패 시 기존 왕관 아이콘으로 폴백.
* **Semantic Red 유지**: 예약 취소 상태, 삭제 버튼, 유효성 오류, 미확인(N) 뱃지, 임박 알림 등 의미상 빨강은 `red-*` 그대로 유지하여 프라이머리 컬러와 분리.
* **Accent Color (Amber)**: `#D97706` - 중요 알림, 대기 상태 뱃지, GNB 알림 카운트.
* **Background**: `#F3F4F6` - 대시보드 배경.

## 3. 통신 프로토콜 (API Interface)

### 3.1. 표준 응답 포맷 (Standard Response)
`Util.createResponse()`를 통해 아래 JSON 구조 준수.
```json
{
  "success": boolean,
  "data": any,
  "message": string
}
```

### 3.2. Util 메서드 정의
* **`Util.createResponse(success, data, message)`**: 표준 응답 객체 생성.
* **`Util.getSheetDataAsObjects(sheetName)`**: 시트 데이터를 JSON 객체 배열로 반환.

## 4. 데이터베이스 스키마 (Google Sheets)

모든 테이블은 데이터 무결성 및 이력 관리를 위해 `created_at`, `updated_at` 컬럼을 기본적으로 포함합니다.

### A. user (사용자 관리)
| Column | Type | Description | 
| ----- | ----- | ----- | 
| `id` | UUID | Primary Key | 
| `user_email` | String | 구글 계정 이메일 (로그인 ID 대체, Unique) | 
| `user_name` | String | 사용자 표시 이름 | 
| `role` | Enum | `admin`(전체), `manager`(지점), `viewer`(조회) | 
| `enabled` | Boolean | 계정 활성화 여부 | 
| `created_at` | Datetime | 생성일 | 
| `updated_at` | Datetime | 수정일 | 

### B. reservation (예약 정보)
| Column | Type | Description | 
| ----- | ----- | ----- | 
| `id` | UUID | Primary Key | 
| `response_id` | String | 설문지 응답 ID (Responses 시트 FK) | 
| `booking_request_date` | Datetime | 예약 요청 일시 (설문 제출 시간) | 
| `branch_id` | UUID | 지점 ID (Foreign Key) | 
| `reservation_date` | Datetime | 방문 예정 일시 | 
| `customer_name` | String | 고객명 | 
| `pax` | Number | 예약 인원 (구 number_of_people) | 
| `notes` | String | 고객 요청사항 (메모) | 
| `phone_number` | String | 고객 연락처 | 
| `email` | String | 고객 이메일 주소 | 
| `email_thread_id` | String | 관련 Gmail 스레드 ID | 
| `calendar_id` | String | 등록된 구글 캘린더 ID | 
| `event_id` | String | 등록된 캘린더 이벤트 ID | 
| `status` | String | 예약 상태 ('pending': 대기, 'confirm': 예약, 'cancel': 취소) | 
| `is_read` | Boolean | 관리자 확인 여부 | 
| `message_sent_at` | Datetime | 최근 알림톡 발송 일시 | 
| `internal_notes` | String | 관리자 메모 (Internal Notes) - 카톡 알림에 포함 | 
| `private_notes` | String | 비공개 메모 (v2.0.75) - 어드민 전용, 카톡 미공유 | 
| `deposit_status` | Enum | `n/a`: 미대상(UI 표기: `N/A`), `pending`: 대기, `confirm`: 확정, `refund`: 환불 | 
| `deposit_amount` | Number | 예약금 금액 (USD) | 
| `deposit_paid_at` | Datetime | 예약금 입금일 | 
| `deposit_refund_at` | Datetime | 예약금 환불일 | 
| `created_at` | Datetime | 데이터 생성일 | 
| `updated_at` | Datetime | 데이터 수정일 | 

### C. branch (지점 정보)
| Column | Type | Description | 
| ----- | ----- | ----- | 
| `id` | UUID | Primary Key | 
| `branch_name_en` | String | 지점명 (영어) - 캘린더 연동 및 식별용 | 
| `branch_name_ko` | String | 지점명 (한글) - UI 표시용 | 
| `address` | String | 지점 주소 (v2.0.75, 메일 예약어 `[[branch_address]]`) | 
| `google_map_link` | String | 구글맵 링크 (v2.0.75, 메일 예약어 `[[branch_google_link]]`) | 
| `enabled` | Boolean | 지점 활성화 여부 | 
| `calendar_id` | String | 지점 전용 구글 캘린더 ID | 
| `created_at` | Datetime | 생성일 | 
| `updated_at` | Datetime | 수정일 | 

### E. slot_master (시간대 마스터)
| Column | Type | Description | 
| ----- | ----- | ----- | 
| `id` | UUID | Primary Key | 
| `time` | Datetime | 예약 가능 시간 (HH:mm 형식) | 
| `slot` | Number | 시스템 기본 슬롯 수 | 
| `enabled` | Boolean | 시간대 사용 여부 | 

### G. slot_override (커스텀 슬롯 - 날짜별)
| Column | Type | Description | 
| ----- | ----- | ----- | 
| `id` | UUID | Primary Key | 
| `branch_id` | UUID | Branch ID (Foreign Key) | 
| `slot_master_id` | UUID | Slot Master ID (Foreign Key) | 
| `date` | Date | 적용 날짜 (YYYY-MM-DD) | 
| `slot` | Number | 재정의할 슬롯 수 | 
| `enabled` | Boolean | 활성화 여부 | 

### H. mail_template (메일 템플릿)
| Column | Type | Description | 
| ----- | ----- | ----- | 
| `id` | UUID | Primary Key | 
| `template_name` | String | 템플릿 식별 이름 (Unique) | 
| `body_html` | String | HTML 메일 본문 | 
| `enabled` | Boolean | 사용 여부 | 
| `created_at` | Datetime | 생성일 | 
| `updated_at` | Datetime | 수정일 | 

### I. duplicate_group (중복 예약 그룹 관리, v2.1.0)
| Column | Type | Description | 
| ----- | ----- | ----- | 
| `id` | String | Primary Key (행 고유 ID, `dg_...`) | 
| `group_id` | String | 중복 그룹 식별자 (`YYYYMMDD_01`, `YYYYMMDD_02` 등 빠른 예약일자_순번) | 
| `reservation_id` | String | 개별 예약 식별자 (`reservation.id`, 1행당 1건 매핑) | 
| `group_type` | Enum | `auto`: 자동 감지 그룹, `manual`: 관리자 수동 묶음 그룹 | 
| `status` | Enum | `active`: 활성(중복 상태), `cleared`: 중복 아님 확인 완료(제외) | 
| `mail_sent_at` | Datetime | 중복 확인 안내 메일 발송 일시 | 
| `created_at` | Datetime | 생성일 | 
| `updated_at` | Datetime | 수정일 | 

## 5. 상세 기능 명세

### 5.1. 공통 기능
* **Custom Alert/Confirm**: 시스템 전반에 통일된 디자인의 커스텀 모달 사용. 컨펌 다이얼로그의 헤더/확인 버튼도 프라이머리 컬러 사용 (v2.0.78에서 앰버에서 변경 — 코퍼 프라이머리와 웜톤이 겹쳐 보이던 문제 해소).
* **Global Spinner**: 초기 로딩 및 주요 처리 시 시각적 피드백 제공.
  * **반투명 오버레이 (v2.0.75)**: 전역 스피너가 화면을 완전히 가리지 않도록 반투명 배경(`bg-white/60`) + 미세 블러(`backdrop-blur`)로 변경하여, 로딩 중에도 뒤 화면 콘텐츠가 비쳐 보이도록 개선. 스피너를 화면 렌더링 분기(`v-else-if`)에서 분리하여 데이터 갱신 중에도 리스트/캘린더 화면이 유지됨.
* **Optimistic UI (v2.0.75)**: 예약 상태·예약금 상태 인라인 변경, 예약 모달 저장, 알림톡 발송 완료 처리, 슬롯 저장(기본/커스텀/일괄)은 서버 응답을 기다리지 않고 로컬 메모리 상태를 즉시 갱신함. 서버 요청은 백그라운드로 전송되며, 실패 시 저장해둔 스냅샷으로 화면을 자동 롤백하고 오류 Alert를 노출함.
* **다중 레이어 모달 ESC 키 계층 순차 닫힘 (Layered ESC Key Stack, v2.1.0)**:
  * 여러 모달이나 컨펌 다이얼로그가 중첩되어 띄워진 경우, `ESC` 키 입력 시 뒤에 깔린 기본 모달(예: 예약 상세)이 예기치 않게 닫히는 현상을 방지하고 z-index 기반의 명확한 레이어 스택 순서에 따라 **항상 가장 상위에 위치한 레이어 하나만 순차적으로 닫힘**.
  * **1순위 (최상위, `z-[1000]`)**: `alertModal` (Alert / Confirm 팝업) $\rightarrow$ 취소(Cancel) 처리와 함께 컨펌창만 닫힘 (뒤의 모달 온전히 유지).
  * **2순위 (`z-[60]`)**: `duplicateMail` (중복확인 메일 스레드 선택 모달) $\rightarrow$ 중복확인 모달만 닫힘 (뒤의 예약 상세 모달 온전히 유지).
  * **3순위**: 모달 내부 상태 드롭다운 / 헤더 드롭다운 $\rightarrow$ 드롭다운만 닫힘.
  * **4순위 (`z-50`)**: 기본 모달 (`reservation`, `system`, `mail`) $\rightarrow$ 다른 중첩 레이어가 모두 닫히고 단독으로 남았을 때만 닫힘.

### 5.2. 예약 리스트 (Tab: List)
* **당겨서 새로고침 (Pull-to-Refresh)**: 모바일 리스트 최상단에서 아래로 당겨 최신 데이터 갱신 지원.
* **검색 & 필터**:
  * 기간, 지점, 상태(대기/예약/취소), 예약금 상태 복합 필터링.
  * 상태별 행 배경색 차별화 (취소: 회색, **대기: 옅은 노란색**).
  * **모바일/데스크톱 필터 동등성**: 모바일 컨트롤(아코디언)에서도 데스크톱과 동일하게 기간·검색·상태·예약금·**지점 필터**를 모두 제공.
* **정렬**:
  * 데스크톱은 테이블 헤더 클릭, 모바일은 정렬 드롭다운(+오름/내림 토글)으로 동일 기준 제공.
  * 정렬 기준: 방문일시, 예약요청일, 고객명, **지점**, **인원**, **상태**, 예약금. (지점 정렬은 `branch_id`를 한글 지점명으로 매핑하여 비교)
* **인라인 상태 변경 (Optimistic)**: 리스트에서 예약 상태 및 예약금 상태 변경 시 확인(Confirm) 직후 로컬 상태를 즉시 반영하고 서버 요청은 백그라운드로 전송함. 네트워크 오류나 예외 실패 발생 시 오류 Alert를 띄우고 이전 데이터로 안전하게 자동 롤백(Rollback)하며, 확인 취소 시에는 전체 데이터 재조회 없이 셀렉트 값만 원복함.

### 5.3. 예약 상세/수정 (Modal)
* **데이터 정합성 보호**: 수정 사항이 있는 상태에서 저장 없이 알림톡 전송 시 **"변경사항 저장 필요"** 안내문 노출.
* **읽음 처리**: 모달 오픈 시 또는 알림톡 전송 시 자동 읽음(`is_read=true`) 처리.
* **상태 관리**: 모달 내부에서 지점을 셀렉트로, 예약 상태(대기/예약/취소)를 헤더 뱃지 클릭 드롭다운으로 수정 후 저장 가능.
* **신규 스레드 생성 (Initialize Mail Thread)**: 외부 위젯 오류 등으로 예약 확정 메일 스레드가 확보되지 않은 경우, 수동으로 예약 확정 내역(기본 영문 양식)을 고객 이메일로 발송하여 새로운 메일 스레드를 강제 생성하고 연동함.
  * **비동기 라벨링**: 신규 스레드 생성 즉시 프론트엔드 클라이언트에는 성공 응답을 반환(0초 지연)하고, 백그라운드 서버 API를 통해 1.5초 후 자동으로 `매장/예약/대기` (단체일 경우 `매장/예약/예약금 대기` 포함) 라벨을 추가하여 클라이언트 렌더링을 차단하지 않음.
* **예약금 UI (v2.0.75)**: 예약금 상태와 금액 입력을 2열 가로 배치로 축소. 금액 입력은 우측 정렬이며 **천 단위 콤마 자동 포맷** 적용 — 숫자만 입력 가능(`inputmode=numeric`), 콤마 삽입/제거 시 커서의 논리적 위치 보존, 화살표 키는 콤마를 한 번에 건너뛰고 정착 위치는 항상 콤마 왼쪽으로 통일, Del/Backspace는 콤마가 아닌 인접 숫자를 삭제. 모델에는 순수 숫자로 저장되어 서버 payload는 기존과 동일.
* **환불처리 링크 (v2.0.82)**: 예약금 카드 하단에 Squarespace 주문 관리로 이동하는 '환불처리' 버튼 추가. 기기별 타겟 분기(`javascript.html`의 `REFUND_ORDER_URLS`):
  * **데스크톱**: 주문 관리 웹 URL 새 탭 (`/config/commerce/orders`).
  * **Android**: 런처 `intent:`로 Squarespace 앱 실행, 미설치 시 Play Store 폴백. (모바일 웹은 매번 인증코드 로그인을 요구하여 지양)
  * **iOS/iPadOS**: 비공개 스키마 `squarespace-app://`로 앱 실행 (실기기 프로브로 발견). GAS 샌드박스 iframe에서는 앵커에 `target` 속성이 있으면(`_blank`뿐 아니라 `_self`도) WebKit이 스키마 네비게이션을 차단하므로, iOS일 때는 `target` 속성을 아예 렌더링하지 않음(`refundTarget`이 `null` 반환). 미설치 폴백/App Store 링크는 없음 — 앱 미설치 시 iOS의 "주소 유효하지 않음" 네이티브 에러가 뜨는 것으로 종료(관리자 폰엔 앱이 설치돼 있어 미설치는 엣지 케이스). iPadOS는 UA가 Macintosh로 위장하므로 `maxTouchPoints`로 판별.
  * **제약**: 모바일은 앱 실행까지만 가능하고 주문 화면 딥 라우팅은 불가 — Squarespace 앱이 유니버설 링크(AASA `applinks` 없음)와 스키마 경로/`?url=` 파라미터 라우팅을 모두 구현하지 않음 (실기기 검증 완료).
* **예약금 상태**: 예약금 관리 영역은 인원 수와 무관하게 항상 노출되어, 9인 미만 예약도 필요 시 수동으로 예약금 상태·금액을 설정할 수 있음. 상태 선택지는 `N/A`(미대상)·대기·확정·환불. (예약금 상태 표기는 시스템 전반에서 `N/A`로 통일. 9인 이상 자동 예약금 적용은 [6.3](#63-예약금-정책) 참조)
* **저장 시 라벨 동기화**: 모달에서 예약 상태/예약금 상태를 변경 후 저장하면, 리스트 인라인 변경과 동일하게 Gmail 라벨이 자동 동기화됨 ([6.2](#62-지메일-라벨-동기화) 참조).
* **고객 이메일/연락처 수정 (v2.0.75)**: 기존 읽기 전용이던 고객 이메일·연락처 필드를 수정 가능하도록 변경.
* **원터치 복사 (v2.0.79)**: 고객 이메일·연락처 입력 필드 우측과 메일 스레드 ID 옆에 복사 아이콘 추가. 탭(클릭) 시 전체 값이 클립보드에 복사되고 중앙 토스트로 피드백 (모바일에서 스레드 ID가 잘려 보여도 풀 값 복사, iframe 클립보드 제한 대비 `execCommand` 폴백 포함).
* **비공개 메모 `private_notes` (v2.0.75)**: 관리자 메모(`internal_notes`, 카톡 알림에 포함)와 별도로, **어드민 화면에만 저장되고 카카오톡 메시지·구글 캘린더 설명 어디에도 공유되지 않는** 비공개 메모 컬럼 추가. 리스트 텍스트 검색 대상에 포함됨. (reservation 시트에 `private_notes` 헤더 컬럼 추가 필요)
  * **리스트 표시**: 데스크톱은 별도 컬럼 없이 관리자 메모 셀 좌측에 자물쇠 아이콘(🔒)으로 존재를 표시하고 호버 툴팁으로 내용 확인, 모바일 카드뷰는 관리자 메모 아래 회색 자물쇠 행으로 표시. 전체 내용 편집은 예약 상세 모달에서 수행.
* **메모 자동 높이 (v2.0.75)**: 고객메모/관리자메모/비공개메모 텍스트영역은 기본 1줄 높이로 표시되며, 내용 길이에 따라 자동으로 확장됨 (`v-autogrow` 디렉티브).
* **모달 여백 축소 (v2.0.75)**: 헤더/본문 패딩, 폼 간격, 카드 패딩, 입력 필드 패딩을 축소하여 메모가 비어 있을 때 PC 화면에서 모달 내부 스크롤이 발생하지 않도록 최적화.
* **모바일 하단 액션바 (v2.0.75)**: 모바일에서는 본문의 알림톡 카드 대신 모달 하단에 고정 액션바(완료/전송 + 최근 전송 시각)를 노출하여, 스크롤·탭 전환 없이 엄지 영역에서 즉시 카톡 액션 수행 가능. (탭 구조 대비 전환 탭이 불필요하고, 상단 배치 대비 엄지 도달성이 우수)
* **헤더 뱃지 상태 변경 (v2.0.75)**: 본문의 '예약 상태' 셀렉트를 제거하고, PC/모바일 공통으로 모달 헤더의 상태 뱃지를 클릭(탭)하면 대기/예약/취소 선택 드롭다운이 열림(캐럿 아이콘 힌트, 현재 상태 하이라이트, 외부 클릭 시 닫힘). 저장 버튼을 눌러야 서버에 반영되는 로컬 변경이므로 오조작에 안전함.
* **노트 정규화**: 고객메모(`notes`)·관리자메모(`internal_notes`)·비공개메모(`private_notes`)는 저장(및 신규 예약 이관) 시 연속된 여러 줄바꿈을 단일 줄바꿈으로 축소하고 앞뒤 공백/줄바꿈을 제거함.
* **알림톡 제목 판단 기준 (v2.0.75)**: 알림톡 제목의 `[신규]`/`[변경]` 구분을 기존 `message_sent_at` 존재 여부가 아닌 **예약 상태 기준**으로 판단함 — 취소(`cancel`)=`[취소]`, 대기(`pending`)=`[신규]`, 완료(`confirm`)=`[변경]`. 예약완료→예약대기로 롤백 후 재발송하는 경우에도 `[변경]`이 아닌 `[신규]`로 발송됨.
* **알림톡 메시지 포맷**: 카카오톡 발송 메시지에서 고객메모/관리자메모는 라벨 다음 줄부터 본문을 출력하며, 메시지 앞뒤 공백/줄바꿈은 제거함. 모바일 공유(`navigator.share`) 시 제목을 별도로 넘기지 않아 일부 안드로이드 공유 경로에서 제목이 중복 표기되던 문제를 방지함.
* **UI 최적화**: 
  * 수정 가능 필드는 흰색 배경, 읽기 전용 필드는 회색 배경으로 명확히 구분.
  * 메일 발송 등 모달 내부 작업 시 화면 전체 스피너 대신 모달 내부 스피너만 작동하도록 하여 사용자 경험(UX) 개선.
* **Optimistic 저장 (v2.0.75)**: 모달 저장 및 알림톡 발송 완료 처리는 스피너로 화면을 막지 않고, 모달·예약 리스트의 메모리 객체를 즉시 갱신한 뒤 서버에 백그라운드로 저장 요청을 전송함. 실패 시 저장 전 스냅샷으로 모달/리스트를 자동 롤백하고 오류 Alert를 노출함. (메일 발송·신규 스레드 생성은 실제 발송 결과 확인이 필요하므로 기존 동기 방식 유지)
* **저장 피드백 (v2.0.75)**: ① 저장 버튼은 변경사항이 있을 때만 활성화(`isReservationDirty` 더티 체크)되고 저장 직후 자동 비활성화되어 반영 완료를 시각적으로 알림. ② 저장/인라인 상태변경/카톡 완료 처리 시 화면 중앙에 토스트 메시지("저장되었습니다" 등)를 1.8초간 표시. 카톡 전송 전 "변경사항 저장 필요" 안내도 동일한 더티 체크를 공용으로 사용.
* **메일 템플릿 선택 초기화 (v2.0.75)**: 템플릿 메일 발송 성공 직후와 예약 상세 모달을 새로 열 때 발송 템플릿 셀렉트를 자동으로 초기화하여, 이전 선택 내역이 다른 예약 건 모달에 남아 보이지 않도록 함.

### 5.4. 시스템 설정 (Tab: Settings)
* **지점 리스트 컬럼 개편 (v2.0.76)**: 지점 관리 리스트에서 캘린더 ID 컬럼을 제거하고 주소·구글맵 링크 컬럼을 추가. 긴 값은 말줄임(...) 처리하고, 마우스 호버 시 커스텀 툴팁(fixed 렌더링, 즉시 표시)으로 풀 텍스트를 보여줌. 컬럼 폭은 지점명(영문) 고정폭 축소 + 주소 컬럼 우선 배분으로 조정.
* **모달 UI 통일 (v2.0.75)**: 시스템 설정/메일 템플릿 모달의 저장·닫기 버튼을 모두 상단 고정 헤더로 이동(타이틀+저장+닫기 항상 노출, 본문만 스크롤). 저장 버튼은 예약 모달과 동일하게 변경사항이 있을 때만 활성화되는 더티 체크 적용(사용자/지점/슬롯 단건·일괄/메일 템플릿 전체).
* **탭 네비게이션 최적화**: 사용 빈도를 고려하여 탭 순서를 재배치(슬롯 관리 -> 메일 템플릿 -> 지점 관리 -> 사용자 관리)하고, 최초 진입 시 '슬롯 관리'가 기본으로 표시됨.
* **커스텀 슬롯 일괄 관리**:
  * 특정 날짜/지점의 모든 시간대를 한 번에 수정하는 일괄 입력 기능.
  * **슬롯 수와 활성화 여부(ON/OFF)를 동시 적용** 가능하도록 고도화.
  * **시간대 마감 프리셋 (v2.0.75)**: 일괄수정 모달에 "점심 시간대 마감(17시 이전 슬롯 0/ON)"과 "오후 시간대 마감(17시 이후 슬롯 0/ON)" 원클릭 프리셋 버튼 제공. 적용 후 저장 버튼으로 확정.
  * **안전한 데이터 처리**: 동시성 문제를 방지하기 위해 일괄 저장(Batch API) 방식 적용.
* **Optimistic 슬롯 저장 (v2.0.75)**: 기본/커스텀/일괄 슬롯 저장 시 로컬 메모리(`slotDefaults`/`slotOverrides`)에 즉시 반영하고 모달을 곧바로 닫음. 저장 요청은 백그라운드로 전송되며, "실제 예약 사이트(위젯 캘린더) 반영에는 최대 몇 분이 걸릴 수 있으니 예약 사이트에 직접 접속해 슬롯 상태를 확인하라"는 안내 Alert를 노출함. 서버 저장 실패 시 스냅샷으로 자동 롤백함.
* **단일 슬롯 수정**: 개별 시간대 클릭 시 기존 설정값을 자동으로 불러오며(Prefill), 새로 설정 시 비활성화(OFF)를 기본값으로 제공.

### 5.5. 메일 템플릿 관리 (Tab: Settings > Mail)
* **시각적 편집 (WYSIWYG)**: Summernote 에디터를 탑재하여 HTML 본문을 직접 편집 가능.
* **치환자 시스템 (Placeholder)**:
  * 예약 데이터(고객명, 방문일, 예약금, 예약금 결제 링크 등)를 동적으로 삽입하기 위해 `[[keyword]]` 형식의 치환자 사용.
  * **지원 예약어**: `customer_name`, `branch_name_en`, `pax`, `reservation_date`, `reservation_time`, `deposit_amount`, `deposit_url`, `branch_address`, `branch_google_link` (v2.0.75에서 branch 스키마의 주소/구글맵 링크 추가 — 미사용 `location` 컬럼은 제거. 시트 컬럼명은 `address`/`google_map_link` 유지, 예약어만 `branch_` 접두사), `alt_branch_name_en`, `alt_branch_address`, `alt_branch_google_link` (v2.0.76, 대체 지점 안내용 — 기존 예약어와의 호환을 위해 base 예약어는 변경하지 않음).
  * **치환 엔진 개선 (v2.0.76)**: `replacePlaceholders`가 고정 키 목록 대신 전달된 데이터 키 전체를 순회하도록 변경되어, 신규 예약어 추가 시 서버 코드 수정이 불필요함. (기존 고정 목록 방식에서는 `address` 등 신규 예약어가 치환되지 않던 문제 수정)
  * **대체 지점 안내 발송 (v2.0.76)**: 템플릿 본문에 `[[alt_` 예약어가 포함되어 있으면 예약 모달의 메일 발송 UI에 대체 지점 선택 드롭다운(현재 예약 지점 제외, 활성 지점만)이 자동 노출됨. 선택 없이는 발송 불가. 클라이언트는 `alt_branch_id`만 전달하고 서버(`apiSendTemplatedMail`)가 지점 정보를 조회해 예약어를 치환하므로 **템플릿 ID 하드코딩이 불필요**함 (콘텐츠 기반 감지). 서버에서 지점 조회 실패 또는 필수 정보(영문명/주소/구글맵 링크) 누락 시 **발송을 중단**하고 누락 항목을 명시한 실패 응답을 반환하며, 클라이언트가 오류 Alert로 노출함 (메일 발송은 optimistic이 아닌 동기 방식이므로 발송 전 차단됨).
  * **에디터 뱃지화**: 편집 편의성을 위해 에디터 내에서는 치환자를 뱃지(Badge) UI로 표시하며, 저장 시 자동으로 텍스트 포맷으로 변환하여 DB에 저장.
  * **속성 내 치환자 보존 (v2.0.77)**: 뱃지 변환이 HTML 태그/텍스트를 구분하도록 개선되어, `href="[[alt_branch_google_link]]"`처럼 태그 속성 안의 치환자는 뱃지로 변환하지 않고 원형을 보존함 (속성 안에 span이 삽입되어 HTML이 깨지던 버그 수정). 미리보기에서는 속성 내 치환자를 순수 값으로 치환해 링크가 실제로 동작함.
  * **예약어 버튼 정렬 (v2.0.77)**: 예약어 삽입 버튼을 고객/예약 → 지점 → 예약금 → 대체 지점 순의 의미 그룹으로 정렬.
  * **생성 응답 직렬화 수정 (v2.0.77)**: 템플릿 생성 API가 응답에 Date 객체를 포함해 `google.script.run` 직렬화가 실패(null 응답)하고 무한로딩되던 버그 수정 — 날짜를 ISO 문자열로 변환해 반환하고, 클라이언트 API 래퍼에 null 응답 가드를 추가해 재발 시에도 오류 Alert로 처리됨.
* **미리보기**: 편집 중인 템플릿이 실제 데이터와 결합되었을 때의 결과물을 실시간으로 확인하는 미리보기 기능 제공. 샘플 데이터는 을지로점 실제 지점 정보(영문 지점명/주소/구글맵 링크) 기반 (v2.0.76).
* **삭제 정책**: 템플릿 삭제 시 복구가 불가능한 실제 행 삭제(Hard Delete)를 수행함.
* **생성/수정 구분**: 편집 모달 헤더가 진입 모드에 따라 '새 메일 템플릿'(생성) / '메일 템플릿 수정'(수정)으로 동적 표기됨.

### 5.6. 캘린더 (Tab: Calendar)
* **지점별 캘린더 조회**: 상단 드롭다운에서 '통합 캘린더(마스터)' 또는 특정 지점을 선택. 통합 선택 시 모든 지점 캘린더가 합쳐진 임베드를, 지점 선택 시 해당 지점 전용 캘린더(`calendar_id` 기반 임베드)를 표시함.

### 5.7. 실시간 동적 알림 (GNB)
* **알림 개수 배지 (Badge):** GNB 우측 상단 종 모양 아이콘 위에 현재 알림이 걸린 고유 예약 건수를 배지로 표시함 (0건일 때는 자동 숨김). 동일 예약에 중복 조건이 있어도 하나의 알림 카드로 통합되어 개수가 계산됨.
  * **시인성 개선 (v2.0.75):** GNB 배경 위에서 Red Badge가 묻혀 보이던 문제를 해결하기 위해 배지 색상을 앰버(`bg-amber-400`) 배경 + 프라이머리 다크(`text-primary-900`) 텍스트로 변경. (코퍼 테마 전환 시 프라이머리 팔레트를 따라감)
* **통합 리스트 및 다중 뱃지:** 종 아이콘 클릭 시 하단으로 드롭다운 카드가 열리며, 각 예약 건당 하나의 카드로 통합되어 노출됨. 해당 예약 건이 갖는 모든 미결 조건의 뱃지들(`[예약대기]`, `[예약금대기]`, `[임박]`, `[환불필요]`)이 가로로 동시에 모두 노출되며 설명 텍스트 없이 깔끔하게 정렬됨.
* **실시간 타이머 적용:** 임박 상태인 예약 건의 경우 남은 시간이 고정되지 않고 1분 간격으로 자동 갱신 및 재렌더링됨.
* **상세 연동:** 알림 항목을 클릭하면 해당 예약의 상세/수정 모달(`ModalReservation`)이 즉각 오픈되어 후속 처리를 지원함.
* **반응형 디자인:** 모바일 해상도에서도 알림 리스트 카드가 화면 우측 밖으로 잘리지 않도록 뷰포트 맞춤형 팝업 너비로 자동 조절됨.

## 6. 비즈니스 로직 (Business Logic)

### 6.1. 예약 상태 라이프사이클
* **신규 접수**: 모든 외부 신규 예약은 초기 상태가 **`pending` (대기)**으로 설정됨.
* **자동 예약 확정**: 관리자가 상세 화면이나 리스트에서 **알림톡(카카오톡) 발송 완료** 버튼을 누를 경우, 시스템이 이를 인지하여 상태를 **`confirm` (예약 확정)**로 자동 전환.
* **수동 상태 변경**: 관리자의 필요에 따라 언제든 대기/예약/취소 상태로 전환 가능.

### 6.2. 지메일 라벨 동기화
* 예약 상태 변화에 따라 연동된 지메일 스레드의 라벨이 실시간으로 동기화됨.
  * 대기 (`pending`): `매장/예약/대기`
  * 예약 (`confirm`): `매장/예약/완료`
  * 취소 (`cancel`): `매장/예약/취소`
* **처리 일원화**: 라벨 동기화 로직은 `ReservationService.updateReservation()` 한 곳에서 처리됨. 리스트 인라인 변경(`updateReservationStatus`/`updateDepositStatus`)과 상세 모달 저장이 모두 이 경로를 거치므로, 어느 화면에서 상태/예약금 상태를 바꾸든 동일하게 라벨이 동기화됨. (상태/예약금 상태가 **실제로 변경된 경우에만** 동기화)
* **예약금 라벨**: 예약금 상태 변경 시 별도 라벨 동기화 (`매장/예약/예약금 대기`·`입금`·`환불`), `N/A` 전환 시 예약금 라벨 제거.

### 6.3. 예약금 정책
* **대상**: 9인 이상 예약 시 자동 감지.
* **자동화**: `ReservationBatch` 실행 시 9인 이상이면 `deposit_status='pending'` 설정 및 안내 로직 작동.
* **계산금액**:
  * 9인 이상 ~ 19인 이하: 10만원
  * 20인 이상 ~ 29인 이하: 20만원
  * 30인 이상 ~ 39인 이하: 30만원
  * 40인 이상 ~ 49인 이하: 40만원
  * 50인 이상 ~ 59인 이하: 50만원
  * 60인 이상 : 에러 (메일 자동발송 안함)

### 6.4. 슬롯 확보 및 동기화
* 예약 상태가 `cancel` (취소)가 되면 점유하고 있던 캘린더 이벤트를 즉시 삭제하고 해당 슬롯을 다시 확보함.
* **투트랙(Two-Track) 캘린더 자동 동기화**: 기본 슬롯(Default) 설정 변경 시 앱스 스크립트 실행 시간 제한(6분)을 우회하고 미래 예약(향후 40일)을 안전하게 제어하기 위한 배치 처리 적용. (커스텀 슬롯 설정이 우선 적용됨)
  * **Track 1 (실시간 백그라운드 큐)**: 설정 저장 시 3초 뒤 백그라운드 1회성 트리거(`triggerBackgroundSlotSync`)를 구동시켜 향후 40일 치 캘린더 이벤트를 즉시 동기화.
  * **Track 2 (새벽 롤링 배치)**: 매일 새벽 시간 기반 트리거(`processDailySlotSync`)가 모든 활성 지점의 향후 40일 치 슬롯을 훑으며 누락된 미래 일자를 캘린더에 동기화.
* **Google Calendar Batch API 최적화**: 다량의 캘린더 이벤트 생성/삭제 시 발생하는 API 호출 제한 및 스크립트 실행 지연을 방지하기 위해 단일 API 호출 대신 **Batch Request** 방식으로 묶어서 전송.
  * `multipart/mixed` 페이로드로 여러 요청을 묶음.
  * 한 번에 최대 **50개 단위(Chunk)**로 분할하여 전송하며, 청크 간 지연(500ms)을 주어 Rate Limit을 우회.
  * 백그라운드 큐 처리 시 한 번에 동기화하는 기간을 기존 20일에서 **10일(Window)**로 축소하여 메모리 및 실행 안정성을 극대화.
* **백그라운드 배치 안정성 강화**:
  * **동시성 제어 (LockService)**: 큐(`PropertiesService`) 접근 시 스크립트 락을 적용하여 여러 프로세스가 동시에 데이터를 수정할 때 발생하는 데이터 경합 및 유실을 원천 차단함.
  * **트리거 강제 갱신 정책**: 큐 등록 시 기존에 등록된 트리거(비활성 상태 포함)를 선제적으로 삭제하고 항상 새로운 트리거를 생성하여 확실한 실행을 보장함.
  * **에러 내성 (Resilience)**: 동기화 과정에서 일시적인 API 오류가 발생하더라도 대기 중인 전체 큐를 삭제하지 않고 보존하여, 다음 배치나 트리거 실행 시 재시도할 수 있도록 개선함.
  * **트리거 생성 부하 최적화 (Batch Enqueue)**: 여러 지점의 동기화가 동시에 필요할 때, 지점별로 트리거를 매번 생성/삭제하지 않고 모든 대상을 큐에 먼저 적재한 후 트리거는 단 1회만 갱신하도록 개선하여 구글 API 호출 제한 및 스크립트 중단(Canceled) 문제를 해결함.

### 6.5. Gmail 스레드 ID 보정 로직 (Auto-Repair)
* **대상**: `email_thread_id`가 누락된 최근 **24시간 이내**의 활성 예약 건.
* **배치 실행**: `ReservationBatch` 실행 시마다 위 대상을 필터링하여 최대 **10건**씩 재검색 및 보정을 시도함.
* **목적**: Gmail의 검색 인덱싱 지연으로 인해 최초 등록 시 ID를 확보하지 못한 경우를 사후 복구하여 시스템 무결성을 보장함.
* **누락 메일 재발송**: 스레드 ID를 뒤늦게 확보한 경우, 최초 배치에서는 스레드가 없어 예약금 안내 메일이 무조건 누락되므로 보정 시점에 재발송함. 예약금 대기(`deposit_status='pending'`, 9인 이상) 건에 한하며, 80인 이상은 예약금 URL이 없어 최초 배치와 동일하게 발송에서 제외함.

### 6.6. 실시간 동적 알림 조건 및 생명주기
* **아키텍처:** 별도의 DB(시트) 쓰기 작업을 수행하지 않고, 프론트엔드가 메모리에 동기화하여 갖고 있는 `reservations` 데이터를 1분 주기 갱신 주기 및 실시간 타이머와 연계해 동적으로 필터링함. 동일 예약 건에 여러 조건이 중복 만족될 시 단일 라인에 여러 뱃지가 함께 표시되며, 남은 시간은 1분 주기로 실시간 갱신됨.
* **동일 예약건 중복 뱃지 처리 규칙:**
  * **시각적 정렬 순서 (좌측부터 우측):** `[임박]` $\rightarrow$ `[예약대기]` $\rightarrow$ `[예약금대기]` $\rightarrow$ `[환불필요]` 순서로 가로 배치됨.
  * **대표 테마 및 아이콘 결정 우선순위:** 하나의 예약 건에 여러 알림 성격이 겹치는 경우, 알림 카드의 대표 아이콘 및 테마 색상(GNB 벨 리스트 좌측 서클 영역)은 가장 긴급한 상태를 기준으로 지정함.
    * **1순위 (최우선):** `임박` (빨간색 타이머 아이콘)
    * **2순위:** `예약대기` (노란색 시계 아이콘)
    * **3순위:** `예약금대기` (주황색 신용카드 아이콘)
    * **4순위:** `환불필요` (파란색 동전/손 아이콘)
* **알림 유형별 조건 및 소멸(해제) 시점:**

| 알림 유형 (뱃지 텍스트) | 대상 조건 | UI 표시 형태 | 소멸(해제) 조건 |
| :--- | :--- | :--- | :--- |
| **`[👥 중복 의심]`** (노란 뱃지, v2.1.0) | - 방문 시각이 오늘 이후 (`date >= today`) <br>- 상태가 대기/확정 (`pending`, `confirm`) <br>- 동일 이메일, 고객명 또는 수동 묶음으로 2건 이상 겹침 | **[👥 중복 의심 (N건)]** 그룹 카드 노출, 중복 건수 및 요약 내역 노출 | - 그룹 내 예약들이 1건 이하로 남을 때 (취소 등) <br>- 관리자가 **[중복아님]** 처리하거나 수동 그룹을 해제할 때 |
| **`[⚠️ 이메일 오류]`** (빨간 뱃지, v2.1.0) | - 방문 시각이 오늘 이후 (`date >= today`) <br>- 예약 상태가 취소가 아님 (`status !== 'cancel'`) <br>- 이메일 정규식 실패, 도메인 오타(gmial 등), 또는 메일 발송 실패(Mailer-Daemon 반송) 감지 | **[⚠️ 이메일 오류]** 뱃지 및 오류 원인(오타 추천 또는 반송 경고) 노출 | - 예약 모달에서 올바른 이메일로 수정 저장할 때 <br>- 예약 상태가 취소(`cancel`)되거나 방문일시가 경과할 때 |
| **`[임박]`** (빨간 뱃지) | - 현재 시간 기준 향후 6시간 이내 방문 예정 <br> `0 <= date - now <= 6 hours` <br>- 예약 상태가 대기 (`status === 'pending'`) | **[임박]** 뱃지 및 남은 시간(1분마다 갱신) 노출 | - 방문 예정 시각이 경과할 때 (`date < now`) <br>- 예약 상태가 **확정(`confirm`)** 또는 **취소(`cancel`)**로 변경될 때 |
| **`[예약대기]`** (노란 뱃지) | - 방문 시각이 미래 (`date > now`) <br>- 예약 상태가 대기 (`status === 'pending'`) | **[예약대기]** 뱃지 노출 | 예약 상태가 **확정(`confirm`)** 또는 **취소(`cancel`)**로 변경될 때 |
| **`[예약금대기]`** (주황 뱃지) | - 방문 시각이 미래 (`date > now`) <br>- 예약금 상태가 대기 (`deposit_status === 'pending'`) | **[예약금대기]** 뱃지 노출 | 예약금 상태가 **확정(`confirm`)**, **환불(`refund`)**, 또는 **미대상(`n/a`)**으로 변경될 때 |
| **`[환불필요]`** (파란 뱃지) | - 방문 시각이 과거 (`date < now`) <br>- 예약금 상태가 완료 (`deposit_status === 'confirm'`) | **[환불필요]** 뱃지 노출 | 예약금 상태가 **환불(`refund`)** 또는 **미대상(`n/a`)**으로 변경될 때 |

### 6.8. 메일 스레드 초기화 및 템플릿 발송 라우팅 최적화
* **수동 스레드 초기화 (`createDummyElfsightThread`)**:
  * 관리자가 수동으로 생성한 초기 메일의 경우, 고객이 지메일 UI에서 답장 클릭 시 자기 자신에게 회신하는 부작용을 방지하기 위해 `replyTo`에 고객 이메일을 지정하지 않음 (`To: 고객`, `Cc: 관리자`).
* **템플릿 기반 답장 발송 (`replyToThreadWithTemplate`)**:
  * **스레드 타입별 동적 라우팅**:
    * **수동 초기화 스레드 (`isDummyThread`)**: 첫 번째 메일이 관리자가 발송한 메일이므로, `messages[0].replyAll()`을 실행하여 원본 수신자인 고객(`To`)과 관리자(`Cc`)에게 전달되도록 분기.
    * **Elfsight 연동 스레드**: 첫 번째 메일이 Elfsight 봇이 보낸 메일(`Reply-To: 고객`)이므로, `messages[0].reply()`를 실행하여 `Reply-To`에 지정된 고객에게 전달되도록 분기.
  * **본문 용량 최적화 (v2.0.74)**: 기존 이메일 본문 하단에 이전 메일 본문을 수동으로 누적 결합하던 인용구(`quoteHtml`)를 제거하여, 스레드가 누적될 때 발생하던 Google Apps Script `GmailApp` 본문 용량 제한(200KB) 초과 에러(`Limit Exceeded: Email Body Size`)를 원천 방지함. (지메일 네이티브 스레딩 헤더 `In-Reply-To`/`References`로 대화 타임라인 유지)
  * **중복 전송 제거**: 불필요한 `cc: SYSTEM_EMAIL_ADDRESS` 중복 지정 옵션을 제거하여 네트워크 페이로드 및 수신함 혼선을 방지함.

### 6.9. 중복 의심 예약 알림 & 중복 그룹 관리 (v2.1.0)
* **목적**: 동일 고객이 여러 지점 또는 동일 지점에 중복으로 예약 신청하는 사례(노쇼 유발)를 조기에 감지하고, 관리자가 편리하게 비교·확인·안내 메일 발송 및 취소 조치를 취할 수 있도록 지원.
* **감지 조건 및 클러스터링 알고리즘**:
  * **자동 감지 풀**: 방문 시각이 오늘 이후(`reservation_date >= today`)이고, 상태가 확정 또는 대기(`status in ('pending', 'confirm')`)인 예약.
  * **일치 조건**: 동일 이메일(대소문자 무시), 또는 동일 고객명(공백 제거, 2글자 이상), 또는 동일 전화번호.
  * **다중 중복 및 그룹 분리 (Disjoint-Set Connected Components)**: A와 B가 동일 이메일이고, B와 C가 동일 고객명인 경우 하나의 중복 그룹(A-B-C)으로 자동 클러스터링되며, 서로 다른 고객군의 중복 그룹들은 완벽히 독립된 그룹 카드로 분리됨. (2건, 3건, 4건 이상 다중 예약 완벽 지원)
* **전용 데이터베이스 시트 (`duplicate_group`)**:
  * 예약 시트의 컬럼을 불필요하게 늘리지 않고 독립된 관계형 시트 구조 채택.
  * `group_type`: `auto`(시스템 자동 감지), `manual`(관리자 수동 묶음).
  * `status`: `active`(중복 활성), `cleared`(관리자가 중복 아님 확인 완료하여 알림 영구 제외).
  * `mail_sent_at`: 고객에게 중복 확인 메일을 발송한 일시 기록.
* **중복 예약 그룹 접기/펼치기 아코디언 & 일괄 토글 (v2.1.0)**:
  * 예약 리스트 및 중복 의심 필터 화면에서 각 그룹 헤더 우측의 캐럿 버튼을 클릭/탭하여 개별 중복 그룹을 부드럽게 접거나 펼칠 수 있음 (데스크톱 및 모바일 완벽 지원).
  * 상단 필터 영역에 `[모든 그룹 접기 / 펼치기]` 일괄 토글 버튼을 제공하여 대량의 중복 예약 그룹이 감지되었을 때 전체 화면 조망 및 탐색 효율성 극대화.
* **중복 비교 모달 (`ModalDuplicateDiff.html`)**:
  * 묶여 있는 예약 건들의 방문일시, 지점, 인원, 예약자명, 이메일, 전화번호, 상태, 예약금, 고객 메모, 관리자 메모를 가로 열로 나란히 비교(Side-by-side spec comparison).
  * **단건 액션**: 각 예약 열 하단에서 `[예약 상세]` 모달 열기, `[그룹에서 제외]`(단건만 중복 그룹에서 분리), `[예약 취소]`(원클릭 취소) 지원.
  * **그룹 전체 액션**: `[중복 아님 처리]`(그룹 전체를 cleared 상태로 변경하여 알림에서 영구 제외), `[수동 그룹 해제]`(수동 그룹 삭제).
* **스레드 기반 중복 확인 메일 전용 발송 (`ModalDuplicateMailSelect.html`, v2.1.0)**:
  * 예약 상세 모달 상단 배너의 `[확인메일 발송]` 버튼 또는 리스트 화면 그룹 헤더의 `[확인메일 발송]` 버튼 클릭 시 전용 스레드 선택 모달이 호출됨.
  * **스레드 유무 기반 선택 통제**:
    * `email_thread_id`가 연동된 예약 건만 `메일발송 가능` (녹색 뱃지)으로 활성화되어 회신 대상으로 선택 가능.
    * 스레드가 없는 예약 건은 `메일발송 불가 (스레드 없음)` (회색 뱃지)으로 표시되어 선택이 차단됨으로써 전송 실패 및 유령 스레드 생성 부작용을 원천 차단.
  * **정형화된 영문 환대 템플릿 (`GmailService.sendDuplicateConfirmationMail`)**:
    * 사무적이거나 장황한 문구를 배제하고 품격 있는 영문 환대 톤앤매너 적용:
      > "Dear [고객명],\nThank you for choosing Wangbijib.\nWe noticed that multiple bookings have been made under your contact details.\nTo ensure everything is perfectly prepared, could you please let us know which booking you would like to keep?\n\n[중복 예약 카드 목록]\n\nBest regards,\nWangbijib Team"
    * 각 예약 내역을 깔끔한 카드 테이블(예약번호, 지점, 날짜/시간, 인원)로 시각화하여 회신에 포함.
  * **발송 이력 및 상태 반영 (v2.1.2)**:
    * 메일 발송 진행 중에는 리스트 화면 그룹 헤더에 `[ 🔄 발송 처리 중... ]` 스피너 뱃지가 실시간 노출되어 전송 진행 피드백을 제공.
    * 발송 완료 시 백엔드 API 응답으로 전달된 발송일시(`mail_sent_at`)를 메모리에 즉시 반영하여 화면 재조회 대기 없이 0초 만에 `✉️ 안내메일 발송됨 (YYYY-MM-DD HH:mm)`으로 전환(Optimistic UI).
* **수동 중복 묶음 (Manual Binding)**:
  * 예약 리스트 좌측 체크박스를 통해 2건 이상의 예약을 선택하면 화면 하단에 플로팅 액션 바가 노출되며, `[👥 선택 예약을 중복 그룹으로 묶기]` 버튼으로 즉시 수동 그룹(`manual_...`) 생성 가능.
* **백엔드 정기 배치 및 멱등성 보장 (`ReservationBatch.run10MinuteBatch`, v2.1.0)**:
  * 프론트엔드의 무거운 그룹핑 연산 부담을 해소하기 위해 10분 주기 백그라운드 Time-driven 트리거 배치로 백엔드에서 중복 그룹을 주기적 동기화.
  * 기존 `created_at` 최초 생성일시 및 `mail_sent_at` 메일 발송 이력을 100% 보존하는 멱등성(Idempotent) 업데이트 보장.

### 6.10. 이메일 오류 종합 감지 및 도메인 오타/반송 처리 (v2.1.0 ~ v2.1.1)
* **목적**: 고객이 예약 위젯에서 이메일을 잘못 입력(도메인 오타 또는 아이디 오타로 인한 메일 반송)하여 예약 확인 메일이 전달되지 못하는 사고를 사전/사후 다층 방어로 완벽 차단.
* **통합 검증 및 반송 탐지 아키텍처**:
  * **사전 형식 & 도메인 오타 검증**:
    * 정규식 문법 검사: `@` 및 도메인 누락, 특수문자 오류 탐지.
    * 대표적 포털 오타 사전 매핑 (`EMAIL_TYPO_MAP`): `gmial.com` $\rightarrow$ `gmail.com`, `naver.con` $\rightarrow$ `naver.com`, `daum.con` $\rightarrow$ `daum.net`, `icould.com` $\rightarrow$ `icloud.com` 등 20여 종 사전 탑재.
  * **사후 발송 실패 (Mailer-Daemon 반송) 자동 탐지**:
    * 고객이 아이디 부분을 잘못 적어 전송 후 메일이 튕겨 나간 경우, Gmail 시스템에 유입되는 `Delivery Status Notification (Failure)` 또는 `Address not found` 반송 메일을 `GmailService.getBouncedEmailDetails`로 1회 일괄 수집(CacheService 5분 캐시 적용).
    * 미래 비취소 예약(`reservation_date >= today`, `status !== 'cancel'`)의 `email_thread_id` 및 `email`과 자동 대조하여 반송 여부 판별.
* **단일 카테고리(`[⚠️ 이메일 오류]`) 완전 통합 UI**:
  * 카테고리를 분리하지 않고 기존의 `[ ⚠️ 이메일 오류 ]` 필터 및 GNB 알림 카드 하나로 일원화하여 관리자 업무 혼선 방지.
  * **GNB 알림 & 리스트 필터**:
    * `[ ⚠️ 이메일 오류 (N) ]` 버튼 하나로 오타 건과 반송 건이 통합 필터링됨.
    * 세부 사유 표시: 도메인 오타 건은 `도메인 오타 의심 (@gmial.com → @gmail.com)`, 반송 건은 `메일 발송 실패 (반송: 없는 계정)`으로 명확히 구분 안내.
  * **예약 상세 모달 안내 및 주소 변경 & 확정메일 재발송**:
    * 도메인 오타: 추천 이메일 표시 및 추천 주소 클릭(또는 [적용하기] 탭) 시 입력창에 즉시 자동 반영.
    * 메일 반송: 붉은색 경고 배너와 함께 "수신 메일함이 존재하지 않거나 거부됨. 고객 연락처로 유선 확인 후 올바른 이메일로 수정해주세요." 유선 대응 가이드 제공.
    * **주소 변경 및 확정메일 재발송 (상시 노출)**: 도메인 오타 감지 여부와 무관하게 버튼이 상시 노출되며, 관리자가 입력창에서 수동 수정한 주소(또는 추천 주소 적용본)를 기준으로 시트 DB 저장 + 고객 확정메일 재발송 + 신규 스레드 연결을 단일 트랜잭션으로 원클릭 수행.
    * **지메일 라벨 상태 동기화 (v2.1.1)**: 신규 스레드 생성(`initializeMailThread`) 및 주소변경 재발송(`handleCorrectEmailAndSendConfirmation`) 시, 현재 예약의 상태(`status`: confirm/cancel/pending) 및 예약금 상태(`deposit_status`: confirm/pending/refund)를 감지하여 신규 스레드의 Gmail 라벨(`매장/예약/완료`, `매장/예약/취소`, `매장/예약/대기`, 예약금 입금/대기/환불)을 정확히 동기화.
    * **이슈 필터 뱃지 카운트 Optimistic UI 즉시 반영 (v2.1.1)**: 주소 변경 재발송, 스레드 초기화, 또는 예약 저장 완료 시 메모리 내 반송(bounced) 데이터를 즉각 제거하고 반응성을 강제 트리거하여, 모달을 닫는 즉시 리스트 테이블뿐만 아니라 상단 `[ ⚠️ 이메일 오류 (N) ]` 및 `[ 👥 중복 의심 (N) ]` 필터 뱃지 숫자가 실시간으로 차감 반영됨.
  * **자동 소멸 규칙**:
    * 관리자가 유선 통화 후 올바른 이메일로 수정 저장하거나, 예약이 취소되거나 방문 일시가 경과하면 자동으로 오류 목록에서 해제 소멸됨.

### 6.11. 중복 확인 메일 발송 및 UX 고도화 (v2.1.1 ~ v2.1.2)
* **이모지 인코딩 보장 (v2.1.1)**: Google Apps Script 환경에서 4바이트 UTF-16 이모지(`🥩✨`)가 깨지는 현상을 원천 방지하기 위해 `_encodeEmojisToEntities`를 통한 HTML Entity(`&#129385;&#10024;`) 자동 치환 발송.
* **레이아웃 폭 확장 및 가독성 최적화 (v2.1.1)**: 이메일 본문 컨테이너 폭을 `max-width: 680px`로 확대하고, 예약 카드 내 지점명/일시/인원수가 좁은 화면에서도 줄바꿈 없이 깔끔하게 한 줄로 출력되도록 최적화.
* **본문 좌측 정렬 및 표준 환대 푸터 규격 통일 (v2.1.2)**:
  * 본문 및 카드가 중앙 정렬되던 문제를 해결하고 왼쪽 정렬(`margin: 0`, `text-align: left`)로 시각적 안정감 복원.
  * 마무리 서명(`Wangbijib Support Team`) 및 하단 공식 푸터 테이블(왕비집 브랜드 로고, 지점 영문 소개, 온라인 스토어, 예약 바로가기, 인스타그램, 고객센터 이메일 및 유선 전화번호)을 기존 확정/안내 메일 서식과 100% 동일한 HTML 규격으로 통일 적용.
* **발송 중 로딩 스피너 및 발송시간 즉시 반영 (v2.1.2)**:
  * 관리자가 중복 확인 메일 발송을 누르면 백엔드 전송이 진행되는 동안 리스트 화면 그룹 헤더에 `[ 🔄 발송 처리 중... ]` 스피너가 실시간 표시됨.
  * 백엔드 API에서 전송 완료 직후 포맷된 발송일시(`mail_sent_at`)를 즉시 반환하며, 프론트엔드는 전체 재조회를 기다리지 않고 즉시 반영하여 0초 만에 `✉️ 안내메일 발송됨 (YYYY-MM-DD HH:mm)` 상태로 전환.

### 6.12. 스키마 자동 마이그레이션 서비스 (MigrationService, v2.1.0)
* **목적**: Google Apps Script 환경에서 신규 버전 배포 시 수동 시트 생성 작업 없이도 신규 테이블이 자동 프로비저닝되도록 보장 (`lululala-lunch-mate` / `jsog` 아키텍처 패턴 적용).
* **동작 원리**:
  * `MigrationService.REQUIRED_SHEETS`에 필수 시트(`duplicate_group`) 및 컬럼 스키마 정의.
  * 스프레드시트에 해당 시트가 없으면 자동 생성 후 헤더 행 설정.
  * 이미 시트가 존재할 경우 누락된 헤더 컬럼만 감지하여 뒤에 자동 추가(기존 데이터 무손실 보존).
  * WebApi의 `apiRunSchemaMigration` 엔드포인트 및 `DuplicateGroupService` 최초 호출 시 멱등성(Idempotent)을 보장하며 자동 실행.

### 6.7. 예약 위젯 커스텀 규칙 (Elfsight Integration)
예약 솔루션 위젯(Elfsight)에 연동되어 동작하는 커스텀 스크립트(`widget/elfsight/CustomJs.js`)는 다음과 같은 핵심 기능들로 구성되어 위젯 단의 입력값 검증 및 영업 정책을 강제합니다.

#### 1) 지점별 최대 예약 인원 검증 및 필수값 유효성 체크
* **인원수(Pax) 유효성 검증:**
  - 사용자가 입력한 예약 인원이 각 지점별로 설정된 최대 예약 인원 수(`maxPax`, 예: 종로점 64명, 시청점 75명 등)를 초과하는 경우, 입력창 바로 아래에 실시간 경고 메시지(`⚠️ Maximum capacity exceeded (Max X for this branch).`)를 노출하고 테두리를 빨간색으로 변경합니다.
  - 인원수가 0 이하이거나 정수가 아닌 경우에도 에러 메시지를 표시합니다.
* **필수 입력값 게이팅 (Confirm Booking 버튼 제어):**
  - 이름, 이메일, 전화번호, 인원수(Pax)가 모두 유효하게 입력되기 전까지는 예약 확정 버튼(`Confirm Booking`)을 비활성화(`disabled = true`, 투명도 0.5, 이벤트 차단) 처리합니다.
  - **전화번호 입력 완료 검증:** Elfsight의 `ProgressiveMask` 텍스트 형태를 비교 분석하여, 전화번호 자리수가 마스크 자리수와 완벽히 일치하거나 국가코드 형식에 부합하는 경우에만 올바르게 입력된 것으로 판단합니다.

#### 2) 단체 예약(9인 이상) 보증금 안내 모달
* **안내 팝업 인터셉트:**
  - 사용자가 9인 이상의 단체 예약을 시도하고 `Confirm Booking` 버튼을 누르면, 예약을 즉시 진행하지 않고 단체 예약금(보증금) 정책 안내 모달을 화면에 띄웁니다.
  - **안내 메시지:** "단체 예약(9인 이상)의 경우 예약금이 필요합니다. 예약금을 납부하시기 전까지는 예약이 **확정 대기** 상태이며, 메일로 전송되는 링크를 통해 결제를 완료하셔야 예약이 확정됩니다."
  - **사용자 제어:** `Cancel` 버튼 클릭 시 팝업이 닫히며 입력 화면으로 돌아가고, `Proceed` 버튼을 누르면 팝업이 닫히며 실제 예약 요청이 최종 전송됩니다.

#### 3) 특정 지점 예약 비활성화 (Branch Disabler)
* **지점 차단:**
  - `STORE_CONFIGS`에 `disabled: true`로 설정된 특정 지점(예: 명동본점 `Wangbijib Myeongdong Main Store Branch`)은 위젯의 지점 목록 카드 내 예약 버튼이 비활성화됩니다.
  - 버튼 문구를 `Not Available`로 변경하고 투명도를 0.5로 낮추며 클릭 이벤트를 차단합니다.

#### 4) 소개멘트 자동 펼치기 (Auto-expand About Section)
* **상세 설명 확장:**
  - 위젯 로드 시 사용자가 지점 소개 문구를 더 읽기 위해 매번 클릭해야 하는 번거로움을 덜어주고자, 더보기 버튼(`.es-text-shortener-control` 중 `Read more` 텍스트를 가진 요소)을 탐색하여 자동으로 클릭 처리(Auto-click)합니다.

#### 5) 규칙 기반 시간대 슬롯 관리 (Break Time & Opening Hours)
* **시간대 차단 및 숨김 규칙:**
  - 지점별 운영 규칙(`rules`)을 적용하여 특정 요일의 시간대 슬롯을 비활성화하거나 보이지 않게 처리합니다.
  - **종로점(Jongno Branch):**
    - **평일(월~금요일):** 14:00 ~ 16:59 (Break Time) 슬롯을 비활성화(`disable` 처리, 불투명도 0.5 및 `(Break Time)` 라벨 추가).
    - **주말(토~일요일) 및 공휴일:** 00:00 ~ 16:59 (런치 시간대 전체) 슬롯을 화면에서 완전 숨김(`hide` 처리, `display: none !important`). 즉, 디너(17:00이후) 예약만 접수 가능합니다.
  - **시청점(City Hall Branch):**
    - **전요일(월~일요일):** 14:00 ~ 16:59 시간대를 비활성화(`disable` 및 `(Break Time)` 라벨 표기) 처리합니다.
* **빈 카테고리 숨김:**
  - 특정 시간대 그룹(예: Morning, Afternoon, Evening) 내의 모든 슬롯이 숨김 처리되면, 해당 그룹의 카테고리 타이틀 역시 함께 자동으로 숨김 처리하여 깔끔한 레이아웃을 유지합니다.
* **공휴일 감지 및 요일 오버라이드:**
  - 위젯 캐러셀 상단의 날짜 헤더 영역(`.es-current-day-content`, 예: `Sunday, July 19` 또는 첫날 `Today, July 17`) 텍스트를 파싱하여 월(Month)과 일(Day)을 추출하고, 오늘 날짜를 기반으로 연도를 계산(연도 미표시 제약 극복 및 12월->1월로 넘어가는 경우 `+1` 연도 롤오버 자동 적용)하여 날짜를 특정합니다.
  - 특정된 날짜가 정의된 **2026년~2027년 대한민국 공휴일(및 대체공휴일)** 목록에 해당하는 경우, 가상 요일 코드를 `0`(일요일, 즉 주말)으로 오버라이드하여 주말 운영 규정을 즉각 강제 적용시킵니다.