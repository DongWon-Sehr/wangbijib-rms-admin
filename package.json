{
  "name": "wangbijib-rms-admin",
  "version": "2.0.76",
  "private": true,
  "description": "왕비집 예약관리 시스템 (Google Apps Script)",
  "scripts": {
    "version:sync": "node scripts/sync-version.js",
    "push": "npm run version:sync && clasp push"
  }
}
